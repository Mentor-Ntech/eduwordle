/**
 * Wallet Context - Re-exports from wallet-context-bridge
 * This maintains backward compatibility with existing imports
 * while using the real RainbowKit/Wagmi integration
 */
export { useWallet, WalletContextBridge as WalletProvider } from './wallet-context-bridge'
